import { DateDurationPipe } from './date-duration.pipe';

describe('DateDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new DateDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
